-- The godown on Turner Road, where every route starts and ends (approximate point from OpenStreetMap; change it in Settings).
update public.settings set data = data || '{"godown": [30.27847, 78.00318]}';
